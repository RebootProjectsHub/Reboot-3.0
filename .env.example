# Contact form (Web3Forms — https://web3forms.com)
# Optional: the access key can instead be pasted directly into
# components/contact-form.tsx (ACCESS_KEY). It is public by design.
# NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=

# Note: the site is a static export hosted on GitHub Pages, so there is no
# server. The old RESEND_API_KEY is no longer used.
